name: macOS build and tests
on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - master
  schedule:
    - cron: '38 2 * * *'

jobs:
  mac-build:
    name: macOS (${{ matrix.arch }})
    timeout-minutes: 30
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-15
            arch: arm64
          - runner: macos-15-intel
            arch: x86_64

    steps:
      - uses: actions/checkout@v7
      - name: configure
        run: >
          mkdir build && cd build &&
          cmake .. 
          -DBUILDING_TESTS=1
          -DCMAKE_BUILD_TYPE=Debug
          -DCMAKE_COMPILE_WARNING_AS_ERROR=ON
          -DCMAKE_OSX_ARCHITECTURES=${{ matrix.arch }}
      - name: build
        run: cmake --build build
      - name: test
        run: cd build && ctest --output-on-failure --output-junit junit.xml
      - name: Prepare JUnit for Codecov
        if: ${{ !cancelled() }}
        run: bash scripts/prepare_junit_for_codecov.sh build/junit.xml ctest.junit.xml ctest
      - name: Upload test results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/codecov-action@v7
        with:
          fail_ci_if_error: true
          files: ctest.junit.xml
          report_type: test_results
          flags: unit-macos-${{ matrix.arch }}
          name: unit-macos-${{ matrix.arch }}
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
      - name: Upload junit.xml
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v7
        with:
          name: unit_macos_${{ matrix.arch }}_junit
          path: ctest.junit.xml
          if-no-files-found: warn
          retention-days: 10
